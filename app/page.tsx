import Link from 'next/link';
import { ArrowRight, BrainCircuit, Network, ShieldCheck } from 'lucide-react';

export default function HomePage() {
  return <main className="min-h-screen overflow-hidden"><nav className="mx-auto flex max-w-7xl items-center justify-between border-b border-[var(--border)] px-6 py-5"><div className="flex items-center gap-3"><span className="flex size-9 items-center justify-center bg-[var(--red)] text-black"><ShieldCheck className="size-5" /></span><span className="font-semibold tracking-[.24em]">FORTEXA</span></div><Link href="/login" className="text-sm text-[var(--muted)] transition hover:text-white">Secure access <ArrowRight className="ml-1 inline size-4" /></Link></nav><section className="mx-auto grid max-w-7xl gap-16 px-6 pb-24 pt-24 lg:grid-cols-[1.15fr_.85fr] lg:items-center"><div><p className="mb-5 text-xs uppercase tracking-[.24em] text-[var(--red)]">Network intelligence / world model</p><h1 className="max-w-3xl text-5xl font-semibold leading-[1.02] tracking-tight sm:text-7xl">Forecast the threat before impact.</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-[var(--muted)]">FORTEXA turns authorized telemetry into network states, future-state simulations, attack trajectories, and defensible SOC decisions.</p><Link href="/login" className="mt-9 inline-flex items-center gap-2 bg-[var(--red)] px-5 py-3 text-sm font-semibold text-black transition hover:bg-white">Enter platform <ArrowRight className="size-4" /></Link></div><div className="grid gap-3 sm:grid-cols-3 lg:grid-cols-1"><Signal icon={<Network />} title="Observe" text="PCAP, NetFlow, IPFIX and CSV become validated network state." /><Signal icon={<BrainCircuit />} title="Simulate" text="Temporal and graph representations drive multi-step forecasts." /><Signal icon={<ShieldCheck />} title="Act" text="Risk, MITRE context and provenance guide investigation." /></div></section></main>;
}

function Signal({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <article className="border border-[var(--border)] bg-[var(--surface)] p-5"><div className="mb-4 text-[var(--red)]">{icon}</div><h2 className="font-medium">{title}</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{text}</p></article>; }