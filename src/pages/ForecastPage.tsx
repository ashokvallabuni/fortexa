import { Clock3, Play, ShieldAlert, TrendingUp } from 'lucide-react';
import { useState } from 'react';

export function ForecastPage() {
  const [horizon, setHorizon] = useState(5);

  return <main className="mx-auto max-w-6xl space-y-5 p-4 text-[var(--foreground)] md:p-7"><header className="flex flex-wrap items-end justify-between gap-4 border-b border-[var(--border)] pb-5"><div><p className="text-[10px] uppercase tracking-[.2em] text-[var(--primary-blue)]">World model / prediction</p><h1 className="mt-2 text-2xl font-semibold">Attack Forecast</h1><p className="mt-1 text-xs text-[var(--muted-foreground)]">Multi-step forecasts appear after an authorized inference job completes.</p></div><button disabled className="flex items-center gap-2 bg-[var(--secondary-blue)] px-3 py-2 text-xs font-medium text-white opacity-50"><Play className="size-3.5" />Run forecast</button></header><section className="grid gap-3 sm:grid-cols-3">{[3, 5, 10].map((value) => <button key={value} onClick={() => setHorizon(value)} className={`border p-4 text-left ${horizon === value ? 'border-[var(--primary-blue)] bg-[var(--soft-blue)]/20' : 'border-[var(--border)] bg-[var(--card)]'}`}><p className="text-lg font-semibold">{value}</p><p className="mt-1 text-[10px] uppercase tracking-wider text-[var(--muted-foreground)]">forecast steps</p></button>)}</section><section className="border border-dashed border-[var(--border)] bg-[var(--card)] p-10 text-center"><ShieldAlert className="mx-auto size-5 text-[var(--muted-foreground)]" /><h2 className="mt-4 text-sm font-semibold">No forecast results available</h2><p className="mx-auto mt-2 max-w-md text-xs leading-5 text-[var(--muted-foreground)]">Process a network dataset and connect the inference worker to populate S(t+1) through S(t+{horizon}) with attack probability, target, confidence, MITRE mapping, and recommended action.</p><p className="mt-4 inline-flex items-center gap-2 text-[10px] text-[var(--muted-foreground)]"><Clock3 className="size-3.5" />Waiting for a real forecast job</p></section><div className="flex items-center gap-2 text-xs text-[var(--muted-foreground)]"><TrendingUp className="size-3.5 text-[var(--primary-blue)]" />No synthetic forecasts are displayed.</div></main>;
}
